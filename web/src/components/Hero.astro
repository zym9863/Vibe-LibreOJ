---
const REPO_URL = 'https://github.com/zym9863/Vibe-LibreOJ';
const LOJ_URL = 'https://loj.ac';
---

<section class="hero">
  <div class="hero-glow"></div>
  <div class="section-wrap">
    <div class="hero-grid">
      <div class="hero-left reveal">
        <div class="prompt-line">
          <span class="prompt">$</span>
          <span class="typed-text" id="typed"></span>
          <span class="cursor"></span>
        </div>
        <h1>
          <span class="title-accent">Vibe</span><br />
          <span class="title-main">LibreOJ</span>
        </h1>
        <p class="subtitle">
          AI Agent 驱动的自动化工作流。<br />
          抓取题目，解析题意，生成 <span class="highlight">11 种语言</span>的解答代码。
        </p>
        <div class="actions">
          <a href={REPO_URL} target="_blank" rel="noopener" class="btn-gh">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
            <span>Source Code</span>
          </a>
          <a href={LOJ_URL} target="_blank" rel="noopener" class="btn-loj">
            LOJ &rarr;
          </a>
        </div>
      </div>
      <div class="hero-right reveal reveal-d3">
        <div class="terminal-window">
          <div class="terminal-bar">
            <span class="terminal-dot red"></span>
            <span class="terminal-dot yellow"></span>
            <span class="terminal-dot green"></span>
            <span class="terminal-title">vloj ~ solve</span>
          </div>
          <div class="terminal-body">
            <div class="term-line"><span class="term-prompt">$</span> vloj solve loj001</div>
            <div class="term-line dim"><span class="term-arrow">&rarr;</span> Fetching problem from loj.ac/p/1...</div>
            <div class="term-line dim"><span class="term-arrow">&rarr;</span> Parsing: A + B Problem</div>
            <div class="term-line dim"><span class="term-arrow">&rarr;</span> Generating solutions...</div>
            <div class="term-line success"><span class="term-check">&#10003;</span> C++ &middot; Python &middot; Rust &middot; Go</div>
            <div class="term-line success"><span class="term-check">&#10003;</span> Java &middot; Kotlin &middot; C# &middot; F#</div>
            <div class="term-line success"><span class="term-check">&#10003;</span> C &middot; Pascal &middot; Haskell</div>
            <div class="term-line"><span class="term-prompt">$</span> <span class="cursor-block"></span></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  const text = "vibe-loj --solve --all-languages";
  const el = document.getElementById("typed");
  if (el) {
    let i = 0;
    const type = () => {
      if (i < text.length) {
        el.textContent += text[i];
        i++;
        setTimeout(type, 45 + Math.random() * 35);
      }
    };
    setTimeout(type, 800);
  }
</script>

<style>
  .hero {
    position: relative;
    z-index: 1;
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 6rem 0 4rem;
    overflow: hidden;
  }

  .hero-glow {
    position: absolute;
    top: -20%;
    left: -10%;
    width: 60%;
    height: 60%;
    background: radial-gradient(ellipse, rgba(0, 255, 136, 0.06) 0%, transparent 70%);
    pointer-events: none;
    z-index: 0;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .hero-left {
    position: relative;
  }

  .prompt-line {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    color: var(--text-muted);
    margin-bottom: 1.5rem;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .prompt {
    color: var(--accent);
    font-weight: 600;
  }

  .typed-text {
    color: var(--text-secondary);
  }

  .cursor {
    display: inline-block;
    width: 8px;
    height: 18px;
    background: var(--accent);
    animation: blink 1s step-end infinite;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  h1 {
    font-family: var(--font-mono);
    font-size: clamp(2.8rem, 5.5vw, 4.5rem);
    font-weight: 700;
    line-height: 1.05;
    margin-bottom: 1.5rem;
    letter-spacing: -0.02em;
  }

  .title-accent {
    color: var(--accent);
    text-shadow: 0 0 40px var(--accent-glow);
  }

  .title-main {
    color: var(--text-primary);
  }

  .subtitle {
    font-family: var(--font-serif);
    font-size: 1.1rem;
    color: var(--text-secondary);
    line-height: 1.8;
    max-width: 440px;
    margin-bottom: 2.5rem;
  }

  .highlight {
    color: var(--warm);
    font-weight: 600;
  }

  .actions {
    display: flex;
    gap: 1rem;
    align-items: center;
  }

  .btn-gh {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 10px 22px;
    background: var(--accent);
    color: var(--bg-deep);
    font-family: var(--font-mono);
    font-size: 0.85rem;
    font-weight: 600;
    border-radius: 6px;
    transition: all 0.2s;
    box-shadow: 0 0 20px var(--accent-dim);
  }

  .btn-gh:hover {
    opacity: 1;
    box-shadow: 0 0 30px var(--accent-glow);
    transform: translateY(-1px);
    color: var(--bg-deep);
  }

  .btn-loj {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    color: var(--text-secondary);
    padding: 10px 20px;
    border: 1px solid var(--border-bright);
    border-radius: 6px;
    transition: all 0.2s;
  }

  .btn-loj:hover {
    border-color: var(--accent);
    color: var(--accent);
    opacity: 1;
  }

  /* Terminal window */
  .hero-right {
    position: relative;
  }

  .terminal-window {
    background: var(--bg-surface);
    border: 1px solid var(--border-bright);
    border-radius: 10px;
    overflow: hidden;
    box-shadow:
      0 0 60px rgba(0, 255, 136, 0.04),
      0 20px 60px rgba(0, 0, 0, 0.5);
  }

  .terminal-bar {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 12px 16px;
    background: var(--bg-elevated);
    border-bottom: 1px solid var(--border);
  }

  .terminal-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  .terminal-dot.red { background: #ff5f56; }
  .terminal-dot.yellow { background: #ffbd2e; }
  .terminal-dot.green { background: #27c93f; }

  .terminal-title {
    margin-left: 8px;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-muted);
  }

  .terminal-body {
    padding: 1.25rem 1.5rem;
    font-family: var(--font-mono);
    font-size: 0.82rem;
    line-height: 1.9;
  }

  .term-line {
    white-space: nowrap;
  }

  .term-line.dim {
    color: var(--text-muted);
  }

  .term-line.success {
    color: var(--accent);
  }

  .term-prompt {
    color: var(--accent);
    font-weight: 600;
    margin-right: 8px;
  }

  .term-arrow {
    color: var(--warm);
    margin-right: 4px;
  }

  .term-check {
    margin-right: 4px;
  }

  .cursor-block {
    display: inline-block;
    width: 8px;
    height: 14px;
    background: var(--accent);
    animation: blink 1s step-end infinite;
    vertical-align: middle;
  }

  @media (max-width: 840px) {
    .hero-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .hero {
      min-height: auto;
      padding: 4rem 0 2rem;
    }

    h1 {
      font-size: clamp(2.2rem, 8vw, 3rem);
    }
  }
</style>
