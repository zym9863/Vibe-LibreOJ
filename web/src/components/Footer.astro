---
const REPO_URL = 'https://github.com/zym9863/Vibe-LibreOJ';
const year = new Date().getFullYear();
---

<footer class="footer">
  <div class="section-wrap">
    <div class="footer-line"></div>
    <div class="footer-grid">
      <div class="footer-left">
        <span class="footer-brand">Vibe LibreOJ</span>
        <p class="footer-desc">AI Agent 驱动的 LibreOJ 题目抓取与多语言解答生成器</p>
      </div>
      <div class="footer-right">
        <div class="footer-col">
          <span class="footer-col-title">Links</span>
          <a href={REPO_URL} target="_blank" rel="noopener">GitHub</a>
          <a href="https://loj.ac" target="_blank" rel="noopener">LibreOJ</a>
        </div>
        <div class="footer-col">
          <span class="footer-col-title">Tech</span>
          <a href="https://chromedevtools.github.io/devtools-protocol/" target="_blank" rel="noopener">CDP</a>
          <a href="https://astro.build" target="_blank" rel="noopener">Astro</a>
        </div>
      </div>
    </div>
    <div class="footer-bottom">
      <span>&copy; {year} Vibe LibreOJ &middot; <a href={`${REPO_URL}/blob/main/LICENSE`} target="_blank" rel="noopener">MIT</a></span>
      <span class="footer-sig">built by AI Agent</span>
    </div>
  </div>
</footer>

<style>
  .footer {
    position: relative;
    z-index: 1;
    padding: 0 0 2rem;
    margin-top: 4rem;
  }

  .footer-line {
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--accent), transparent);
    margin-bottom: 3rem;
    opacity: 0.3;
  }

  .footer-grid {
    display: flex;
    justify-content: space-between;
    gap: 3rem;
    margin-bottom: 3rem;
  }

  .footer-left {
    max-width: 340px;
  }

  .footer-brand {
    font-family: var(--font-mono);
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--accent);
  }

  .footer-desc {
    margin-top: 0.75rem;
    font-family: var(--font-serif);
    font-size: 0.85rem;
    color: var(--text-muted);
    line-height: 1.6;
  }

  .footer-right {
    display: flex;
    gap: 3rem;
  }

  .footer-col {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .footer-col-title {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.25rem;
  }

  .footer-col a {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--text-muted);
    transition: color 0.2s;
  }

  .footer-col a:hover {
    color: var(--accent);
    opacity: 1;
  }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border);
    font-family: var(--font-mono);
    font-size: 0.72rem;
    color: var(--text-muted);
  }

  .footer-sig {
    color: var(--text-muted);
  }

  @media (max-width: 640px) {
    .footer-grid {
      flex-direction: column;
      gap: 2rem;
    }

    .footer-bottom {
      flex-direction: column;
      gap: 0.5rem;
      text-align: center;
    }
  }
</style>
